import { StatusCodes } from 'http-status-codes';

const getAllProPlayers = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const getProPlayerById = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const createProPlayer = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const updateProPlayer = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const deleteProPlayer = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const getAdsForProPlayer = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const getGamesForProPlayer = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const AssignGameToProPlayer = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const UpdateProPlayerGameDetails = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const getProPlayerReviews = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

export default {
    getAllProPlayers,
    getProPlayerById,
    createProPlayer,
    updateProPlayer,
    deleteProPlayer,
    getAdsForProPlayer,
    getGamesForProPlayer,
    AssignGameToProPlayer,
    UpdateProPlayerGameDetails,
    getProPlayerReviews
}