import { StatusCodes } from 'http-status-codes';

const getAllGames = (req, res, next) => {
    try {
        
    } catch (error) {
        next(error);
    }
}

const getGameById = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const createGame = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const updateGame = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const deleteGame = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

const getAdsForGame = (req, res, next) => {
    try {

    } catch (error) {
        next(error);
    }
}

export default {
    getAllGames,
    getGameById,
    createGame,
    updateGame,
    deleteGame,
    getAdsForGame
}